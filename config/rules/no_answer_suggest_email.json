{
  "enabled": true,
  "phase": "post",
  "priority": 100,
  "description": "When one or more user requests cannot be answered reliably, suggest contacting a human by email even if other requests in the same message were answered.",
  "when": {
    "type": "result_state",
    "field": "has_unanswered_requests",
    "operator": "eq",
    "value": true
  },
  "then": [
    {
      "type": "suggest_agent",
      "agent": "send_email",
      "label": "Send an email",
      "post-message": "you can click to this link to",
      "requires_confirmation": true
    }
  ]
}
